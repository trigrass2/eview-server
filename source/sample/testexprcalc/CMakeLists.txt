cmake_minimum_required(VERSION 2.6)

PROJECT (testexprcalc)
SET(SRCS ${SRCS} testexprcalc.cpp)

SET_SOURCE_FILES_PROPERTIES(${SRCS} COMPILE_FLAGS -DCPPUNIT_FRIEND_CLASS)

SET(TARGET_NAME testexprcalc)

SET(LINK_LIBS ${LINK_LIBS}  exprcalc)

INCLUDE(../../CMakeCommonExec)
#SET(EXECUTABLE_OUTPUT_PATH ${EXECUTABLE_OUTPUT_PATH}/${UNITTEST_DIR})

IF(MSVC)
	if( CMAKE_SIZEOF_VOID_P EQUAL 8 )
		set_target_properties(${TARGET_NAME} PROPERTIES STATIC_LIBRARY_FLAGS "/machine:x64")
	endif( CMAKE_SIZEOF_VOID_P EQUAL 8 )
ENDIF(MSVC)
